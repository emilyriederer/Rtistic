# Rtistic

The goal of Rtistic is to ...

## Installation

You can install the released version of Rtistic from [CRAN](https://CRAN.R-project.org) with:

``` r
install.packages("Rtistic")
```

## Example

This is a basic example which shows you how to solve a common problem:

``` r
## basic example code
```

